here right view of bst means nothing but the nodes that are visible from the right side of an bst like it is similar to the left view of bst only
 here similar code but slight change

class Node
{
int data;
Node left;
Node right;
Node(int data)
{
this.data=data;
this.left=null;
this.right=null;
}
}
import java.util.*;
class Solution
{
public static ArrayList<Integer> rightview(Node root)
{
ArrayList<Integer>list=new ArrayList<>();
rview(root,list,0);
return list;
}
public static void rview(Node root,ArrayList<Integer>list,int idx)
{
if(root==null) return ;
 if(list.size()==idx)
{
list.add(root.data);
}
rview(root.right,list,idx+1);
rview(root.left,list,idx+1);
}
}
