Problem statement
You have been given a binary tree of integers. You are supposed to find the left view of the binary tree.
The left view of a binary tree is the set of all nodes that are visible when the binary tree is viewed from the left side.
Sample Input 1:
2
3 4 -1 -1 -1 
2 8 7 -1 5 -1 -1 1 -1 -1 -1
Sample Output 1:
3 4
2 8 5 1

class Node
{
int data;
Node left;
Node right;
Node(int data)
{
this.data=data;
this.left=null;
this.right=null;
}
}
import java.util.*;
class Solution
{
public static ArrayList<Integer> getleftview(Node root)
{
ArrayList<Integer>list=new ArrayList<>();
leftview(root,list,0);
return list;
}
public static void leftview(Node root,ArrayList<Integer>list,int index)
{
if(root==null) return ;
if(list.size()==index) list.add(root.data);
leftview(root.left,list,index+1);
leftview(root.right,list,index+1);
}
}
